# ICS202
Treasure Quest is a grid-based pathfinding problem where the goal is to find the shortest safe path from start (S) to treasure (E) while avoiding walls and monster-monitored cells. The solution uses BFS on a grid with preprocessed danger zones to compute the shortest valid route or return IMPOSSIBLE.
